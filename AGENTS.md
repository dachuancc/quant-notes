# AGENTS.md — quant-notes

给 AI 编码助手（pi 等）的项目上下文。**开始改动前，先读 `docs/ROADMAP.md`（进度与下一步）
和 `docs/DECISIONS.md`（已定的架构决策，勿轻易推翻）。**

## 这是什么

量化研究背后的数学与统计笔记（与 backtest-lab 互为表里）

## 技术栈

- Markdown + LaTeX · 零依赖 · 链接校验 CI
- 依赖与虚拟环境用 `uv` 管理

## 目录结构

```
# TODO: 画出主要目录 + 每个文件一句话职责
```

## 常用命令

```bash
uv sync                       # 安装依赖
# TODO: 运行命令
# TODO: 测试命令（改动后必须全绿）
```

## 代码约定

- **改动必须带测试**，且测试全绿才算完成（见 `docs/ROADMAP.md` 的测试基线）。
- **提交信息**：用中文，见下方「提交信息规范」。
- 关键行为（边界、错误、幂等、权限等）都要有守护测试。

## 提交信息规范

**先分两类**：看这次改动是否对应 `docs/ROADMAP.md` 里的里程碑编号。

| 类别 | 格式 | 例子 |
|---|---|---|
| **里程碑功能** | `M<编号>: 简述` | `M2: 新增导出功能` |
| **其他** | `<类型>: 简述`（类型见下表） | `文档: 补充部署说明` |

**类型表**（只用这几个，不要自造）：

| 类型 | 用于 |
|---|---|
| `文档` | 改 `docs/`、`README.md`、`AGENTS.md` |
| `配置` | 依赖、配置、环境变量、构建 |
| `工具` | 开发 / 运维工具链（脚本、脚手架） |
| `重构` | 不改行为的结构调整 |
| `修复` | 修 bug |
| `测试` | 仅增改测试（不含功能改动） |

**正文**：用 `-` 列要点，写**为什么**而不只是做了什么；行为变化要说清（含兼容性影响）。

**规则**
- 用中文，主题行简短（≤ 约 40 字），不以句号结尾。
- **功能开发一律用 `M<n>:`**，编号是 `ROADMAP.md` 的锚点，关系到能否按编号回溯进度。
- 一次提交只做一件事；**文档改动不要混进功能提交**。
- **不追溯历史**：旧提交里的异类写法保留原样，只管新提交按上表执行。

## 当前状态

见 `docs/ROADMAP.md` 的「当前状态」——那里是唯一状态真相，本文件不重复维护。

## 红线

- TODO: 列出**绝对不能做**的事（法律 / 版权 / 安全 / 业务边界）。
