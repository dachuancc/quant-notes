# quant-notes

量化研究背后的数学与统计笔记（与 backtest-lab 互为表里）

## 快速开始

```bash
uv sync                       # 安装依赖
# TODO: 补充运行 / 测试命令
```

## 文档（仓库自描述）

本项目的状态**全部写在文件里**，不依赖对话记忆——新会话（人或 AI）读文件即可接手。

| 文件 | 用途 |
|---|---|
| `AGENTS.md` | AI 助手入口（pi 启动时自动加载）：约定、命令、红线 |
| `docs/ROADMAP.md` | **唯一状态真相**：里程碑、当前状态、下一步、已知局限 |
| `docs/DECISIONS.md` | 关键架构决策与理由，**勿轻易推翻**（要推翻先改文档） |

新会话继续开发：`cd quant-notes && pi`，然后读上面三份（`AGENTS.md` 会自动加载）。
