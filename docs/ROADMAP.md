# ROADMAP / 项目进度 — quant-notes

> 这是本项目的**唯一状态真相**。每次接手先读这里，再看 `docs/DECISIONS.md`。
> 完成一项就在这里勾掉并更新「当前状态」与「下一步」。

## 如何接手（Human & AI）

1. 在项目目录下启动 pi：`cd quant-notes && pi`（pi 会自动加载本目录的 `AGENTS.md`）。
2. 读本文件 + `docs/DECISIONS.md`。
3. 跑测试确认基线全绿（命令见 `AGENTS.md`）。
4. 从下方「下一步」继续。

> ⚠️ **开工前请用新会话**（直接 `pi`），**不要恢复旧会话**（`pi -r` / `pi -c`）。
> 旧会话会把**已被推翻的旧决策**一起带回来，而模型不会报错、只会用错。

## 目标

- TODO: 这个项目要解决什么问题、边界在哪（不做什么）。

## 当前状态（给下一个会话）

- TODO: 一句话说清「现在能干什么 / 做到哪了」。
- 测试基线：**N 个用例**。

## 里程碑

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M0 | TODO | ⬜ |

## 下一步

- TODO

## 已知限制

- TODO
