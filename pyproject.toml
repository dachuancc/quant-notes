[project]
name = "quant-notes"
version = "0.1.0"
description = "量化研究背后的数学与统计笔记"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "CC-BY-4.0" }

[dependency-groups]
dev = ["pytest>=7.4"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
